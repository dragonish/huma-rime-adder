name: Release

on: 
  workflow_dispatch:
  push:
    tags:
      - 'v*'


jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Set Up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller
      - name: Build Windows Package
        run: |
          pyinstaller -i icon.ico -n huma-rime-adder main.py
          copy config.ini.template dist\huma-rime-adder\
      - name: Upload Windows build
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: dist/huma-rime-adder/
  
  build-linux:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Set Up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller
      - name: Build Linux Package
        run: |
          pyinstaller -i icon.ico -n huma-rime-adder main.py
          cp config.ini.template dist/huma-rime-adder/
      - name: Upload Linux build
        uses: actions/upload-artifact@v4
        with:
          name: linux-build
          path: dist/huma-rime-adder/
  
  build-macos-intel:
    runs-on: macos-15-intel
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Set Up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller
      - name: Build macOS Intel Package
        run: |
          pyinstaller -i icon.ico -n huma-rime-adder main.py
          cp config.ini.template dist/huma-rime-adder/
      - name: Upload macOS Intel build
        uses: actions/upload-artifact@v4
        with:
          name: macos-intel-build
          path: dist/huma-rime-adder/

  build-macos-arm64:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Set Up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pyinstaller
      - name: Build macOS ARM64 Package
        run: |
          pyinstaller -i icon.ico -n huma-rime-adder main.py
          cp config.ini.template dist/huma-rime-adder/
      - name: Upload macOS ARM64 build
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64-build
          path: dist/huma-rime-adder/
  
  release:
    needs: [build-windows, build-linux, build-macos-intel, build-macos-arm64]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      - name: Get git tags
        id: git_tags
        run: |
          current=$(git describe --abbrev=0 --tags)
          echo "current=${current}" >> ${GITHUB_OUTPUT}
      - name: Create changelog text
        id: changelog_text
        uses: dragonish/tag-changelog@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          config_file: .github/scripts/tag-changelog-config.cjs
      - name: Download Windows build
        uses: actions/download-artifact@v5
        with:
          name: windows-build
          path: release/windows
      - name: Download Linux build
        uses: actions/download-artifact@v5
        with:
          name: linux-build
          path: release/linux
      - name: Download macOS Intel build
        uses: actions/download-artifact@v5
        with:
          name: macos-intel-build
          path: release/macos-intel
      - name: Download macOS ARM64 build
        uses: actions/download-artifact@v5
        with:
          name: macos-arm64-build
          path: release/macos-arm64
      - name: Create uploads directory
        run: |
          rm -rf uploads
          mkdir uploads
      - name: Zip Windows artifact to uploads
        run: |
          cd release/windows
          zip -r "${GITHUB_WORKSPACE}/uploads/huma-rime-adder_windows_x64_${{ steps.git_tags.outputs.current }}.zip" .
      - name: Zip Linux artifact to uploads
        run: |
          cd release/linux
          zip -r "${GITHUB_WORKSPACE}/uploads/huma-rime-adder_linux_x64_${{ steps.git_tags.outputs.current }}.zip" .
      - name: Zip macOS Intel artifact to uploads
        run: |
          cd release/macos-intel
          zip -r "${GITHUB_WORKSPACE}/uploads/huma-rime-adder_macos_intel_${{ steps.git_tags.outputs.current }}.zip" .
      - name: Zip macOS ARM64 artifact to uploads
        run: |
          cd release/macos-arm64
          zip -r "${GITHUB_WORKSPACE}/uploads/huma-rime-adder_macos_arm64_${{ steps.git_tags.outputs.current }}.zip" .
      - name: Create release
        uses: ncipollo/release-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ steps.git_tags.outputs.current }}
          name: Release ${{ steps.git_tags.outputs.current }}
          prerelease: false
          draft: false
          artifacts: "uploads/*"
          body: ${{ steps.changelog_text.outputs.changes }}
