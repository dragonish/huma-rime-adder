# 项目指南

## 项目概述

这是一个基于 Python 3 语言的用于为 Rime 输入法（虎码等）批量添加/管理词条的 GUI 工具项目，使用 MVC 模式和 PyQt6 框架。

## 开发环境要求

- Python 3.11+
- PyQt6

## 常用命令

### 构建和运行

```bash
# 运行项目
python main.py
```

## 项目结构

```plain
├── main.py              # 主程序入口
├── app/                 # 应用程序管理目录
├── config/              # 配置管理目录
├── common/              # 实用方法目录
├── controller/          # 控制器目录
├── data/                # 数据处理目录
├── log/                 # 日志管理目录
├── model/               # 模型目录
├── type/                # 类型目录
├── view/                # 视图目录
└── window/              # 窗口目录
```

## 开发指南

- 遵循现有的代码风格
- 尽量减少变更并集中精力
- 在引入新结构之前检查现有文件
- 不要重构不相关的代码

## 验证规则

当用户准确询问"规则检查"时，回复： "此项目已加载 AGENTS.md。"

## 语言要求

- 所有与用户的交互必须使用中文
- 代码注释和文档可以使用中文或英文，但优先使用中文以保持一致性
- 当用户使用中文提问时，必须用中文回答
